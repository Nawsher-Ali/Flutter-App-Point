import 'package:flutter/material.dart';

class FourScreen extends StatelessWidget{

  @override
  Widget build(BuildContext context) {
    return Container(
      color:Color(0xFF019031),
      child: Center(
        child: Text('Four Screen',textDirection: TextDirection.ltr,),
      ),
    );
  }
}